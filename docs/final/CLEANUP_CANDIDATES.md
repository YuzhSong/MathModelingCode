# Cleanup Candidates (record only)

本轮只记录，不删除。任何删除前都应先确认没有脚本、报告或复现路径依赖。

| Path / pattern | Why it may be removable | Safety |
|---|---|---|
| `.DS_Store` files under repository | macOS Finder metadata, not research data | likely safe after separate confirmation |
| `__pycache__/` and `.pytest_cache/` if regenerated | generated bytecode/test cache | usually safe |
| `*.tmp` and accidental smoke logs | temporary outputs if not referenced | verify first |
| `logs/` debug/API logs | may contain practice provenance or API evidence | not sure; preserve by default |
| duplicate-looking Q3/Q4 smoke directories | may be needed to explain gate fixes | not sure; keep |
| old W0--W8 scripts and policies | historical research evidence, not disposable clutter | keep |
| old `results/` CSV/report directories | reproducibility and negative evidence | keep |

No candidate was automatically removed.
