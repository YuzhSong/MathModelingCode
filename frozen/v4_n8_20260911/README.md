# Frozen Q3 V4/n=8 baseline

This directory is an immutable snapshot created before V5 work on 2026-09-11.

- Policy baseline: V4 with `n=8`
- Evaluation source: local `offline_sim` practice cases only
- Frozen result set: random seeds `0:100`, stress seeds `10000:10050`
- Stress suites: `min_reff`, `collinear`
- Official formal test: not invoked

The active V5 code must not import from or write into this directory. File hashes
are recorded in `SHA256SUMS`.
