# Frozen Q3 V6/n=8 candidate

This snapshot contains the V6 route-aware supplement policy and its official
practice adapter as of 2026-09-12.

- Policy: V6, outer search point count `n=8`
- Offline status: 200 same-seed cases, 100% clear, 0 clear failures
- Official status: practice validation pending; no official endpoint was called while creating this snapshot
- Formal test: not authorized and not invoked

The default official strategy remains V4. To run this candidate, use the
explicit `--strategy v6` flag after manually confirming the simulator is in
Q3 practice/simulation mode and the safety token is supplied.

`SHA256SUMS` covers every source and instruction file in this snapshot.
